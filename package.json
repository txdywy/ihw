{
  "name": "hotwheels-hub",
  "version": "1.0.0",
  "description": "Hot Wheels collector hub - automated news, releases and gallery",
  "scripts": {
    "scrape": "node scripts/scrape.js",
    "test": "vitest --run",
    "build": "node scripts/build.js",
    "dev": "npm run build && npx serve _site -p 3000"
  },
  "dependencies": {
    "node-html-parser": "^6.1.0"
  },
  "devDependencies": {
    "fast-check": "^4.7.0",
    "vitest": "^4.1.5"
  }
}
